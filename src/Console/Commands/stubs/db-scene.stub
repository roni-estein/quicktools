<?php

use Illuminate\Database\Seeder;

class DummyModel extends Seeder
{
    /**
     * An database atomic action that creates or modifies one item and returns the modification
     */
    public function run($attributes = [])
    {
        // return $user = create(User::class);
    }
}
